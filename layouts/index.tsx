import * as React from 'react';
import { Head, Main, NextScript } from "next/document";

export default () => (
    <html>
        <Head>
            <title>This is title.</title>
        </Head>
        <body>
            <Main/>
            <NextScript/>
        </body>
    </html>
);